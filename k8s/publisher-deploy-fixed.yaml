apiVersion: apps/v1
kind: Deployment
metadata:
  name: publisher-fixed
  namespace: pulsar-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: publisher-fixed
  template:
    metadata:
      labels:
        app: publisher-fixed
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "publisher-fixed"
        dapr.io/log-level: "debug"
        dapr.io/volume-mounts: "oauth-credentials-plain-text:/snp/resources"
    spec:
      volumes:
        - name: oauth-credentials-plain-text
          configMap:
            name: oauth-credentials-plain-text
      containers:
      - name: publisher
        image: us-central1-docker.pkg.dev/strong-harbor-338418/elia-sb-debug-mirel/pulsar-bug:latest
        imagePullPolicy: Never
        volumeMounts:
          - name: oauth-credentials-plain-text
            mountPath: /snp/resources
            readOnly: true
